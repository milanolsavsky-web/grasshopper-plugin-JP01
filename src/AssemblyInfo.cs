// Assembly-level attributes that control how Grasshopper loads this plugin.
// ForceDirect means Grasshopper loads the plugin immediately at startup
// rather than waiting until the user places a component.
//
// You should not need to modify this file.

using Grasshopper.Kernel;

[assembly: GH_Loading(GH_LoadingDemand.ForceDirect)]
